[project]
name = "local-memory-hub"
version = "0.1.0"
description = "Obsidian vault and client integration for Basic Memory"
requires-python = ">=3.12,<3.15"
dependencies = ["basic-memory==0.23.2", "fastmcp==4.0.0b1", "fastmcp-slim==4.0.0b1", "cryptography==50.0.1"]

[tool.uv]
package = false
prerelease = "if-necessary-or-explicit"
