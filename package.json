{
  "name": "@natejswenson/local-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bounded local persistent memory with explicit scope and deletion-aware recovery",
  "engines": {
    "node": ">=25.2.1"
  },
  "bin": {
    "local-memory": "bin/local-memory.mjs",
    "local-memory-adapter": "bin/adapter.mjs"
  },
  "files": [
    "bin",
    "src",
    "adapters",
    "docs",
    "scripts/compatibility.mjs",
    "scripts/host-smoke.mjs"
  ],
  "scripts": {
    "test": "node --test",
    "probe": "node scripts/compatibility.mjs",
    "benchmark": "node scripts/benchmark.mjs"
  }
}
