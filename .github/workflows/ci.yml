# >>> press:gha-header v0.9.0 sha256:43ccb7a7ef36 GENERATED by @natjswenson/press, do not edit
# ══════════════════════════════════════════════════════════════════════════
#  NS ┃ CI                                      linkedin.com/in/natejswenson
# ──────────────────────────────────────────────────────────────────────────
#  Validate repository policy and workflow YAML on every pull request to
#  main.
#  generated by ghfactory 0.2.1 — regenerate, do not hand-edit
# ══════════════════════════════════════════════════════════════════════════
# <<< press:gha-header
name: CI

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, ready_for_review, edited]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  repository-policy:
    name: repository-policy
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"
      - run: |
          python -m pip install PyYAML==6.0.3
      - run: |
          python -B -m unittest discover -s tests -v
      - name: Validate GitHub Actions schema
        run: |
          go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12
          "$(go env GOPATH)/bin/actionlint" .github/workflows/ci.yml .github/workflows/main-automerge.yml
  application:
    name: application
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "26.8.2"
          package-manager-cache: false
      - run: |
          npm ci --ignore-scripts
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: natejswenson/claude-skills
          ref: 446ee69b1cf8b5cf0583694ea0e7744f71ae4f30
          path: node_modules/issueflow-source
          persist-credentials: false
      - env:
          ISSUEFLOW_COMPLETION_MODULE: file://${{ github.workspace }}/node_modules/issueflow-source/skills/issueflow/skills/issueflow/scripts/lib/completion.mjs
        run: |
          node scripts/compatibility.mjs
          node --test
          node scripts/benchmark.mjs
